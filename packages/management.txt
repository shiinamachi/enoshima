# Reprovisioning additions not explicit in the original native package list.
ansible-core

# Local PKGBUILDs declare the `rust` provider as a build dependency. Keep the
# Arch rustup package explicit for bootstrap compatibility while mise remains
# authoritative for the user-facing development toolchain selection.
rustup
