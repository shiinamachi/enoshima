# Reprovisioning additions not explicit in the original native package list.
ansible-core

# rustup was installed as an orphaned dependency, but its active stable
# toolchain is meaningful desired state, so it is normalized to explicit.
rustup
