---
- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Reexecute systemd manager
  ansible.builtin.systemd_service:
    daemon_reexec: true

- name: Reload journald
  ansible.builtin.systemd_service:
    name: systemd-journald.service
    state: reloaded

- name: Flush journal to persistent storage
  ansible.builtin.command: /usr/bin/journalctl --flush
  changed_when: false

- name: Restart atop
  ansible.builtin.systemd_service:
    name: atop.service
    state: restarted
    daemon_reload: true

- name: Restart sysstat collection timer
  ansible.builtin.systemd_service:
    name: sysstat-collect.timer
    state: restarted
    daemon_reload: true

- name: Reload TLP power policy
  ansible.builtin.systemd_service:
    name: tlp.service
    state: reloaded

- name: Run WWAN fallback policy
  ansible.builtin.systemd_service:
    name: wwan-fallback.service
    state: started

- name: Rebuild boot artifacts
  ansible.builtin.command:
    cmd: /usr/local/libexec/enoshima-rebuild-uki
  environment:
    ENOSHIMA_UKI_SECURE_BOOT_SIGNING: >-
      {{ 'true' if (secure_boot_sign_uki | bool) else 'false' }}
  when:
    - manage_boot_config | bool
    - apply_boot_artifacts | bool
  changed_when: true
  notify: Record boot artifact rebuild

- name: Record boot artifact rebuild
  ansible.builtin.copy:
    content: "managed boot inputs applied\n"
    dest: "{{ boot_artifact_state_file }}"
    owner: root
    group: root
    mode: "0644"
