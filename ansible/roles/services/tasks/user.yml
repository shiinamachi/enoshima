---
- name: Enable and start desired user units
  ansible.builtin.systemd_service:
    name: "{{ item }}"
    enabled: true
    state: started
    scope: user
    daemon_reload: true
  loop: "{{ user_units_started }}"
