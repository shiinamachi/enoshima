#!/usr/bin/env bash
set -euo pipefail

if (($# > 1)); then
  printf 'usage: cyberdock-minimize [ADDRESS]\n' >&2
  exit 2
fi

if (($# == 1)); then
  exec desktop-window-action minimize --address "$1"
else
  exec desktop-window-action minimize --active
fi
