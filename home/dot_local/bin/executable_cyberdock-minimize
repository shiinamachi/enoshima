#!/usr/bin/env bash
set -euo pipefail

if (($# > 1)); then
  printf 'usage: cyberdock-minimize [ADDRESS]\n' >&2
  exit 2
fi

if (($# == 1)); then
  exec cyberdock-state minimize "$1"
else
  exec cyberdock-state minimize
fi
